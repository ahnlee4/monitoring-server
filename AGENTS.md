# Repository Working Instructions

이 저장소에서 작업할 때는 아래 규칙을 항상 우선 적용한다.

## Collaboration Rules

- 사용자가 요청한 내용이 애매하거나 이해가 확실하지 않으면, 진행 전에 이해한 내용을 한국어로 다시 설명하고 확인을 받는다.
- 사용자가 명확히 지시한 작업은 가능한 한 끝까지 진행한다.
- 보드 적용, Docker 이미지 빌드/푸시, Git 커밋/푸시처럼 배포에 영향을 주는 작업은 결과와 확인 명령을 함께 정리한다.

## Git Rules

- 기능 변경, UI 수정, 통신 수정, 배포 설정 변경이 생길 때마다 의미 있는 단위로 커밋한다.
- 커밋 메시지는 변경 내용을 한국어로 요약해서 작성한다.
- 원격 저장소 푸시는 사용자가 직접 한다. 명시적으로 요청받은 경우를 제외하고는 커밋까지만 수행한다.
- 기존 작업 내역을 되돌리거나 덮어쓰기 전에 반드시 확인한다.

## Code Organization

- 코드를 한 파일 또는 한 페이지에 계속 몰아넣지 않는다.
- 기능별로 컴포넌트, 모듈, API, 드라이버, 유틸리티를 분리한다.
- 화면 UI, 통신 프로토콜, 백엔드 API, 배포 설정은 서로 역할이 섞이지 않도록 유지한다.
- 같은 로직이 반복되면 공통 함수나 컴포넌트로 분리한다.

## Project Context

- 이 프로젝트는 기존 Java GUI 기반 압축기 모니터링 프로그램을 Ubuntu/RK3566 보드에서 동작하는 Web/Docker 기반 시스템으로 이전하는 작업이다.
- 보드 실행은 `docker-compose.board.yml` 기준으로 관리한다.
- 화면 디자인은 기존 원본 GUI의 구조를 최대한 유지하되, 1280x800 보드 화면에 맞춰 가독성과 사용성을 개선한다.
- 통신은 실제 장비/펌웨어 프로토콜을 우선하며, 더미 데이터나 하드코딩 값이 화면에 표시되지 않도록 주의한다.
